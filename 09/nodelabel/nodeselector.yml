---
kind: Pod
apiVersion: v1
metadata:
  name: pod-nodeselector
spec:
  nodeSelector:
    gpu: "true"
    disk: ssd
  containers:
    - name: nginx-container
      image: nginx
      ports:
      - containerPort: 80
